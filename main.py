# Define the function "on_forever" This sets out what the function does.
def on_forever():
    basic.show_number(199)

# Call the Function to do its job
basic.forever(on_forever)
# Define the function "on_forever" This sets out what the function does.

